# PalDeck v2.3 安全生命周期总实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 依次交付 30 天安全回收站、外部 Mod 取消管理、PalDeck 正式仓库更新、UE4SS 所有权/修复/卸载以及 v2.3.0 发布。

**架构：** 实现拆为四份可独立审查的子计划。Trash 和配置函数是 UE4SS 卸载的前置依赖；更新器与其余后端并行独立；UI 和发布最后统一集成。

**技术栈：** Python 3.13、Flask、原生 ES Modules、CSS、pytest、Node test、PyInstaller、GitHub Releases。

---

## 执行顺序

- [ ] **阶段 0：分支与基线**

```bash
cd F:/Grok_Workspace/01_Projects/palworld-mod-manager/.worktrees/v2.1-workshop-ue4ss-sakura
git status --short
git branch -m feature/v2.3-safety-lifecycle
npm test
./.venv-build/Scripts/python.exe -m pytest -q
```

预期：改名前工作区 clean；Node `33 passed`；Python `585 passed, 6 skipped`。若基线数字因计划文档测试发生变化，只允许新增计划契约测试，不允许存在失败。

- [ ] **阶段 1：回收站与外部 Mod**

执行：`docs/superpowers/plans/2026-07-14-paldeck-v2.3-trash-external-plan.md`

产物：共享 `mods.txt` 配置函数、严格 Trash schema、同卷负载、回收/恢复/清理、外部来源和忽略清单。

- [ ] **阶段 2：自动更新迁移**

执行：`docs/superpowers/plans/2026-07-14-paldeck-v2.3-updater-migration-plan.md`

产物：唯一可信更新仓库 `ylty1516/PalDeck`，旧仓库和相似 URL 全部拒绝。

- [ ] **阶段 3：UE4SS 生命周期**

执行：`docs/superpowers/plans/2026-07-14-paldeck-v2.3-ue4ss-lifecycle-plan.md`

前置检查：`backend/trash_service.py`、`backend/trash_store.py`、`backend/ue4ss_config.py` 已存在且测试通过。

产物：所有权 schema、三来源安装记录、完整性、配置保护、修复、卸载到回收站和恢复。

- [ ] **阶段 4：API/UI/视觉/发布**

执行：`docs/superpowers/plans/2026-07-14-paldeck-v2.3-integration-release-plan.md`

产物：固定 API、外部标签、回收站 dialog、UE4SS 状态卡、忽略重置、20 张基线、v2.3.0 Frozen smoke、两次可复现构建和正式 Release。

## 规格覆盖矩阵

| 规格章节 | 实现计划 |
|---|---|
| 4. TrashService | trash-external 任务 2-4；integration 任务 1、3-4 |
| 5. 外部发现与忽略 | trash-external 任务 5-6；integration 任务 1、3、5 |
| 6. 更新迁移 | updater-migration 全部；integration 任务 7-8 |
| 7. UE4SS Manager | ue4ss-lifecycle 全部；integration 任务 2、5 |
| 8. API | integration 任务 1-2 |
| 9. 前端 | integration 任务 3-6 |
| 10. 迁移 | 四份计划的 schema/兼容测试 |
| 11. 测试 | 每个任务的红绿循环；integration 任务 6-8 |
| 12. 发布 | integration 任务 7-8 |

## 每阶段完成门槛

每个 commit 前必须满足：

```bash
./.venv-build/Scripts/python.exe -m pytest -q
git diff --check
```

每个阶段结束必须满足：

```bash
npm test
./.venv-build/Scripts/python.exe -m pytest -q
for file in frontend/*.js; do node --check "$file"; done
./.venv-build/Scripts/python.exe -m compileall -q backend launcher.py scripts
git status --short
```

任何失败先按 systematic-debugging 定位根因；不得删除测试、放宽安全断言或跳过失败用例。
