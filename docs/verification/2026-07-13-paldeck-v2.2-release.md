# PalDeck v2.2.0 发布验证

## 提交边界

- Packaged source commit：`62bc010535a4d4fb917ef78760b2d169be2ac0f1`
- 分支：`feature/v2.2-concept-ui`
- 发布验证文档提交之后，`source..HEAD` 只允许新增本文件。
- 原仓库 `ylty1516/palworld-mod-manager` 的 `main` 不修改。

## 功能范围

- 五个页面采用响应式蓝白玻璃界面，提供侧栏、底部状态栏和三档布局。
- 无边框 pywebview 窗口只暴露最小化、最大化/还原和关闭白名单；`PALDECK_NATIVE_FRAME=1` 可回退原生标题栏。
- 我的模组显示真实筛选、统计、本地/Workshop 状态和原有安全操作。
- 导入页支持多文件顺序队列；原生文件夹选择只枚举顶层普通 ZIP/PAK，不递归、不跟随 reparse point，以短时随机授权安装。
- Nexus 保持匿名只读，前后端 fail-closed 过滤成人内容，不提供收藏、下载或自动安装。
- 设置、外观、UE4SS、背景、三种樱花和开源致谢均保留。

## 自动化验证

- Python：`575 passed, 6 skipped`。
- Node：`32 passed`。
- 所有 `frontend/*.js` 通过 `node --check`。
- `backend`、`launcher.py`、`scripts` 通过 `compileall`。
- `git diff --check` 通过。
- 15 张视觉基线覆盖五页 × `1600×1000`、`1280×820`、`960×640`；候选与批准基线归一化差异均为 `0.00000000`。
- Edge 截图通过唯一 token ready 握手、超时和进程树清理；含空格输出路径已实测。

## 可复现构建

两次全新 `scripts/build_portable.ps1` 构建结果完全一致：

- EXE：`30,347,384 bytes`
- EXE SHA-256：`2593f7b1098a8e9fd193b78e291758121a10785bd2d3afb329424062cec5c5b8`
- ZIP：`30,146,301 bytes`
- ZIP SHA-256：`c465506abe7538fc83b045b0ae4fed5268e9a712b36f2a97b3a7a29a0004868b`
- ZIP CRC：`OK`
- ZIP sidecar：`OK`
- ZIP 条目：2

## Frozen smoke

真实 `PalDeck.exe`、WebView 与 HTTP 自检共 18 项全部通过：

- 五视图与发布标记
- v2.2 响应式外壳和桌面桥标记
- 空导入队列
- Nexus 成人内容 fail-closed 标记
- Frozen health
- fresh data 无游戏路径
- Workshop 空状态
- Okaetsu 内置 UE4SS 元数据
- Appearance 读取
- 三套主题
- 三种樱花风格
- 樱花 high/off
- 默认 WEBP 背景

Smoke 结束后 owned process tree 已终止。

## 发布后检查

创建正式 `v2.2.0` Release 后必须：

1. 确认非草稿、非预发布、target commit 为最终验证提交。
2. 回下载 ZIP 与 sidecar，执行 `sha256sum -c`。
3. 确认 GitHub 资产摘要等于 `c465506abe7538fc83b045b0ae4fed5268e9a712b36f2a97b3a7a29a0004868b`。
4. 只将 ZIP 复制到 `C:\Users\yyx\Desktop\pal mod`。

## 残余风险

- EXE 未进行商业代码签名，Windows SmartScreen 仍可能提示。
- Windows 字体栅格、DPI 与 WebView2 版本会造成合理的像素差异；基线用于当前固定验证环境的回归检测。
- 应用内更新受信任仓库按既定范围仍为 `ylty1516/palworld-mod-manager`，本版本从 `ylty1516/PalDeck` Release 手动下载。
