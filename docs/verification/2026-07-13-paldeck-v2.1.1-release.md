# PalDeck v2.1.1 发布验证（2026-07-13）

## 验证范围

- 版本：PalDeck v2.1.1 Windows 便携版
- packaged source commit：`bd0fd52d44c71b2f745f7f8034a7c4d6c70cff2a`
- 验证前 HEAD：`bd0fd52d44c71b2f745f7f8034a7c4d6c70cff2a`
- 验证前 Git 工作区：clean
- 构建、运行缓存和中间目录均配置在本仓库所在的 F 盘。
- 本次未推送、未创建 GitHub Release、未复制产物到桌面。

## 两次独立正式构建

两次均正式执行 `scripts/build_portable.ps1`。脚本每次删除并重建 `.venv-build`，使用 Python 3.13.5、锁定且带 hash 的依赖，并从同一 packaged source commit 和 `SOURCE_DATE_EPOCH=1783904991` 构建。

| 项目 | 第一次 | 第二次 |
| --- | --- | --- |
| 日志 | `.tmp/build-v2.1.1-1.log` | `.tmp/build-v2.1.1-2.log` |
| Python 测试 | 549 passed, 5 skipped | 549 passed, 5 skipped |
| Node 测试 | 17 passed, 0 failed | 17 passed, 0 failed |
| EXE bytes | 30,116,425 | 30,116,425 |
| EXE SHA-256 | `dd42d412be7910771f77bd4e583f9950806672d0cc1e5bafa6fdb134c0af5ec5` | `dd42d412be7910771f77bd4e583f9950806672d0cc1e5bafa6fdb134c0af5ec5` |
| ZIP bytes | 29,916,118 | 29,916,118 |
| ZIP SHA-256 | `ebc2bbb49d431fe42d7d55e84f0fb8db6cc7b6d8f5ef64a9543f2b42c2203156` | `ebc2bbb49d431fe42d7d55e84f0fb8db6cc7b6d8f5ef64a9543f2b42c2203156` |

两份产物快照 `.tmp/artifact-v2.1.1-1.txt` 与 `.tmp/artifact-v2.1.1-2.txt` 内容一致，因此两次 EXE 与 ZIP 的 size 和 SHA-256 完全一致。首次构建脚本本身成功；首次命令最后的非零状态来自构建完成后的内联 PowerShell 变量被 Bash 展开，未影响构建和完整测试，产物快照在第二次构建覆盖前单独保存。

## Frozen 便携版 smoke

正式执行 `scripts/smoke_portable.ps1`，日志为 `.tmp/smoke-v2.1.1.log`。结果：

- `ok=true`，报告进程 `frozen=true`，版本 `2.1.1`；
- 从最终 ZIP 解压并启动真实 `PalDeck.exe`，观察到 WebView 窗口句柄；
- 观察到该进程树持有的 `127.0.0.1:12057` HTTP listener；
- 五个视图 marker 全部存在；
- `natural`、`watercolor`、`minimal` 三种樱花风格的 POST/GET 均通过；
- 三套主题、花瓣 high/off、默认 WebP、fresh data、health 均通过；
- Workshop 在未配置游戏路径时返回空列表；
- 内置 UE4SS bundle metadata：6,982,837 bytes，SHA-256 为 `7c80b2f4a29baf0f384552c8517e58196e78c8a1b8530637b7179eddae1b54a9`；
- 15 个 smoke item 全部通过，handshake marker 已删除；
- 只终止本次启动的 PID 28096 及其子进程 PID 4060，结束后 `smoke_process_tree_terminated=true`。

## 在线服务实测

在线检查结果保存在 `.tmp/live-v2.1.1.log`，缓存使用 F 盘仓库内 `.tmp/release-live-cache-v2.1.1`。

- Okaetsu 固定 Release `experimental-palworld`：唯一资产 `UE4SS-Palworld.zip`，6,982,837 bytes，GitHub digest 与 bundle digest 同为 `7c80b2f4a29baf0f384552c8517e58196e78c8a1b8530637b7179eddae1b54a9`，`update_available=false`。
- Nexus `NSFW` 真实 GraphQL raw 返回 24 项，24 项的 `adultContent=true`；同一查询通过 `NexusCatalog` 返回 0 项，且 `source=live`、`stale=false`、无 warning，证明成人内容在 Catalog 输出前被过滤。
- Nexus popular：`source=live`、`stale=false`、无 warning；上游请求 24 项，安全过滤后返回 22 项，首项 ID 为 16。
- Nexus ID 16：`source=live`、`stale=false`、无 warning，返回 `MapUnlocker`，规范 URL 为 `https://www.nexusmods.com/palworld/mods/16`。

## 归档、sidecar 与第三方材料

归档检查结果保存在 `.tmp/archive-v2.1.1.log`：

- ZIP CRC 全量检查通过，`testzip()` 无坏条目；
- ZIP 恰有两个条目：`PalDeck-portable/PalDeck.exe`（30,116,425 bytes，CRC32 `7ce7a897`）与 `PalDeck-portable/README.txt`（945 bytes，CRC32 `9ee00b3b`）；
- `.sha256` sidecar 文件名与 ZIP SHA-256 完全匹配；
- ZIP 内 README 含 `Source commit: bd0fd52d44c71b2f745f7f8034a7c4d6c70cff2a`；
- `third_party/ue4ss-palworld/UE4SS-Palworld.zip` 为 6,982,837 bytes，SHA-256 为 `7c80b2f4a29baf0f384552c8517e58196e78c8a1b8530637b7179eddae1b54a9`，与 manifest 完全一致；
- 随附 `LICENSE` 包含 MIT License 与 `Copyright (c) 2022 Narknon`。

## C 盘与进程残留检查

检查结果保存在 `.tmp/c-drive-v2.1.1.log`：

- 正式 smoke 后没有名为 `PalDeck` 的运行进程；本次拥有的进程树 PID 28096/4060 已全部终止；
- 本次正式 smoke 明确使用 F 盘 `F:\Grok_Workspace\01_Projects\palworld-mod-manager\.worktrees\v2.1-workshop-ue4ss-sakura\.tmp\portable-smoke\runtime-temp\_MEI280962`（创建于 2026-07-13 01:21:30 UTC）；
- C 盘检查发现 `C:\Users\yyx\AppData\Local\Temp\_MEI107282`（创建于 01:12:25 UTC）与 `_MEI108402`（创建于 01:21:50 UTC），均无进程占用。它们不属于正式 smoke 报告的 owned process tree，无法证明由本次正式 smoke 创建；遵循只清理本次进程树的边界，未擅自删除并精确披露；
- 另有验证开始前已存在的 `C:\Users\yyx\AppData\Local\Temp\paldeck-lock-dryrun.txt`（创建于 2026-07-11 15:39:31 UTC），亦未删除。

## 提交边界

packaged source commit 之后只新增本验证文档。可用以下命令独立确认：

```text
git diff --name-status bd0fd52d44c71b2f745f7f8034a7c4d6c70cff2a..HEAD
```

预期仅列出 `docs/verification/2026-07-13-paldeck-v2.1.1-release.md`。

## 残余风险

- EXE 未做 Windows Authenticode 代码签名，首次运行仍可能出现 SmartScreen 提示。
- GitHub 与 Nexus 属实时外部服务；本页只证明 2026-07-13 验证时的响应，后续内容和可用性可能变化。
- PyInstaller 报告 Android backend 与 `pycparser.lextab`/`yacctab` 可选 hidden import warning；Windows frozen smoke 已覆盖实际采用的 Edge WebView 路径。
- 两个无法归属本次 owned process tree 的 C 盘 `_MEI*` 目录仍保留；当前无进程占用，不判定为本次发布阻塞。
