# PalDeck v2.2.3 发布验证

## 根因与修复

- Packaged source：`e6b28e8a8ed3acca84fa77c6b4fbe2d6df8610e6`
- 原实现将 UE4SS 解压和发布暂存到系统临时目录，Steam Library 位于其他盘时，`os.replace` 抛出 Windows `WinError 17`。
- 修复后根据每位用户实际配置的 Palworld `Win64` 目录动态创建 `.paldeck-ue4ss-*` 事务目录，无任何硬编码盘符。
- 解压、备份、发布和回滚始终位于目标游戏所在文件系统，支持任意本地 Steam Library 盘符。
- 成功或失败后事务目录均清理；原有回滚测试继续通过。
- 前端显示安装中、成功或具体失败结果；只有真正可替换的已有安装才弹出替换确认，Workshop 互斥冲突不再错误询问替换。

## 跨盘真实验证

- 系统临时目录：C 盘。
- 项目及模拟 Steam 游戏目录：F 盘。
- 使用固定内置 `UE4SS-Palworld.zip` 真实安装成功。
- 发布文件：20 个。
- 安装后 `status.installed = true`。
- `.paldeck-ue4ss-*` 残留：0。

## 自动化验证

- Python：`584 passed, 6 skipped`。
- Node：`33 passed`。
- JavaScript 语法、Python compileall 与 `git diff --check` 通过。
- 两次完整便携构建完全一致；第二次构建日志包含 `584 passed, 6 skipped` 和 PyInstaller `Build complete!`。
- Frozen smoke：18 项全部通过，owned process tree 已终止。
- ZIP CRC 与 sidecar 校验通过。

## 产物

- EXE：`30,349,802 bytes`
- EXE SHA-256：`84628bf539d3873ed78bbbff7bff3e847753295c979bf265ffd4f2648b5b20d5`
- ZIP：`30,148,880 bytes`
- ZIP SHA-256：`da2ac46e0a2cdd10f127520e8180053dbfe0c797a0a802bd917bc1ca86826430`

## 残余风险

- EXE 未签名，可能触发 SmartScreen。
- 游戏必须退出，且用户需要对 Palworld `Win64` 目录有写权限。
