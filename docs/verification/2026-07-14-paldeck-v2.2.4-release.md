# PalDeck v2.2.4 发布验证

## 修复范围

- Packaged source：`3b3705a078575173bd6222e4be0180e10f139532`
- 根据用户实际 `1236×771` 还原窗口截图新增中间响应式断点。
- `≤1350px` 时模组列表切换为紧凑两行网格，身份、状态、来源、版本和操作均保留。
- 模组操作区允许换行并限制宽度，本地、Workshop 及异常状态的全部按钮不会越出卡片或窗口。
- 顶栏和 Nexus 排序按钮允许安全换行。
- 底部游戏目录文本使用可收缩区域；“打开”按钮固定最小 `52×34`，采用 inline-flex 水平和垂直居中。
- 背景层与应用壳使用明确 stacking context，减少 Edge/WebView2 截图分块异常。

## 视觉验证

- 五页 × 四尺寸，共 20 张批准基线：
  - `1600×1000`
  - `1280×820`
  - 用户实际还原尺寸 `1236×771`
  - 最小尺寸 `960×640`
- 20 张候选与批准基线归一化差异均为 `0.00000000`。
- 人工检查 `1236×771` 与 `960×640`：本地及 Workshop 全部操作按钮可见；底栏“打开”文字居中。

## 自动化验证

- Python：`585 passed, 6 skipped`。
- Node：`33 passed`。
- JavaScript 语法、Python compileall 与 `git diff --check` 通过。
- 两次完整便携构建完全一致，日志均包含 `585 passed, 6 skipped` 和 PyInstaller `Build complete!`。
- Frozen smoke：18 项全部通过，owned process tree 已终止。
- ZIP CRC 与 sidecar 校验通过。

## 产物

- EXE：`30,349,872 bytes`
- EXE SHA-256：`79cdc0ac871a163a81448bff9197e104dbb1daeb247a99e52cd9f13346b2a70d`
- ZIP：`30,148,931 bytes`
- ZIP SHA-256：`95d86b37b3d55f13a2b009214b117ca23c1fe95bc425f781c061252c7828af93`

## 残余风险

- EXE 未签名，可能触发 SmartScreen。
- Windows DPI 与 WebView2 字体栅格可能产生合理的细微像素差异。
