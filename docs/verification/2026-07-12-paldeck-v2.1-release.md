# PalDeck v2.1 发布验证（2026-07-12）

## 验证范围

- 版本：PalDeck v2.1.0 Windows 便携版
- packaged source commit：`17e8649dc98a6960c9651e3f1a6b5e611128bf5a`
- 验证前 Git 工作区：clean
- 构建和运行缓存、中间目录均位于本仓库所在的 F 盘。
- 本次未推送、未创建 GitHub Release、未复制产物到桌面。

## 两次独立正式构建

两次均正式执行 `scripts/build_portable.ps1`。脚本每次删除并重建 `.venv-build`，使用 Python 3.13.5、锁定且带 hash 的依赖，并从同一 packaged source commit 和 `SOURCE_DATE_EPOCH=1783866957` 构建。

| 项目 | 第一次 | 第二次 |
| --- | --- | --- |
| 日志 | `.tmp/build-v2.1-1.log` | `.tmp/build-v2.1-2.log` |
| Python 测试 | 527 passed, 5 skipped | 527 passed, 5 skipped |
| Node 测试 | 17 passed, 0 failed | 17 passed, 0 failed |
| EXE bytes | 30,116,499 | 30,116,499 |
| EXE SHA-256 | `e5d0d59410cec1bfa755964ccf8530b0e742f7bf25641901c21918cd7c627d51` | `e5d0d59410cec1bfa755964ccf8530b0e742f7bf25641901c21918cd7c627d51` |
| ZIP bytes | 29,916,206 | 29,916,206 |
| ZIP SHA-256 | `b7cf3b0fd2876e3e020bf4c53abe19670e50ffae9b797417d9a64b1b27214408` | `b7cf3b0fd2876e3e020bf4c53abe19670e50ffae9b797417d9a64b1b27214408` |

逐字比较 `.tmp/artifact-build-1.txt` 和 `.tmp/artifact-build-2.txt` 无差异，因此两次 EXE 与 ZIP 的 size 和 SHA-256 完全一致。

## Frozen 便携版 smoke

正式执行 `scripts/smoke_portable.ps1`，日志为 `.tmp/smoke-v2.1.log`。结果：

- `ok=true`，报告进程 `frozen=true`，版本 `2.1.0`；
- 从最终 ZIP 解压并启动真实 `PalDeck.exe`；观察到 WebView 窗口句柄；
- 观察到该进程树持有的 `127.0.0.1:5419` HTTP listener；
- 五个视图 marker 全部存在；
- `natural`、`watercolor`、`minimal` 三种 `petal_style` 的 POST/GET 均通过；
- 三套主题、花瓣 high/off、默认 WebP、fresh data、health 均通过；
- Workshop 在未配置游戏路径时返回空列表；
- 内置 UE4SS bundle metadata digest 为 `7c80b2f4a29baf0f384552c8517e58196e78c8a1b8530637b7179eddae1b54a9`；
- 15 个 smoke item 全部通过；handshake marker 已删除；
- 只终止本次启动的 PID 29212 及其子进程 PID 19576，结束后 `smoke_process_tree_terminated=true`。

## 在线服务实测

在线检查结果保存在 `.tmp/live-v2.1.log`，缓存使用 F 盘仓库内 `.tmp/release-live-cache`。

- Okaetsu 固定 Release `experimental-palworld`：唯一资产 `UE4SS-Palworld.zip`，6,982,837 bytes，GitHub digest 与 bundle digest 同为 `7c80b2f4a29baf0f384552c8517e58196e78c8a1b8530637b7179eddae1b54a9`，`update_available=false`。
- Nexus popular：`source=live`、`stale=false`、无 warning，返回 24 项；首项 ID 为 16。
- Nexus ID 16：`source=live`、`stale=false`、无 warning，返回 `MapUnlocker`，规范 URL 为 `https://www.nexusmods.com/palworld/mods/16`。

## 归档、sidecar 与第三方材料

归档检查结果保存在 `.tmp/archive-v2.1.log`：

- ZIP CRC 全量检查通过，`testzip()` 无坏条目；
- ZIP 恰有两个条目：`PalDeck-portable/PalDeck.exe`（30,116,499 bytes，CRC32 `5da47133`）与 `PalDeck-portable/README.txt`（945 bytes，CRC32 `3bb66df9`）；
- `.sha256` sidecar 文件名与 ZIP SHA-256 完全匹配；
- ZIP 内 README 含 `Source commit: 17e8649dc98a6960c9651e3f1a6b5e611128bf5a`；
- `third_party/ue4ss-palworld/UE4SS-Palworld.zip` 为 6,982,837 bytes，SHA-256 为 `7c80b2f4a29baf0f384552c8517e58196e78c8a1b8530637b7179eddae1b54a9`，与 manifest 完全一致；
- 随附 `LICENSE` 包含 MIT License 与 `Copyright (c) 2022 Narknon`。

## C 盘与进程残留检查

检查结果保存在 `.tmp/c-drive-v2.1.log`：

- smoke 后没有名为 `PalDeck` 的运行进程；
- `C:\Users\yyx\AppData\Local\Temp` 与 `C:\Windows\Temp` 中未发现本次构建或 smoke 创建的 `_MEI*` 或 `PalDeck*` 中间目录/EXE；
- 发现一个验证开始前已存在的 `C:\Users\yyx\AppData\Local\Temp\paldeck-lock-dryrun.txt`（创建于 2026-07-11 15:39:31 UTC，最后写入于 15:39:50 UTC）。为遵守只清理本次进程树的边界，本次未删除该历史 dry-run 文本；其内容是旧的 pip dry-run 输出，不是可执行文件或本次产物。

## 提交边界

packaged source commit 之后只新增本验证文档。可用以下命令独立确认：

```text
git diff --name-status 17e8649dc98a6960c9651e3f1a6b5e611128bf5a..HEAD
```

预期仅列出 `docs/verification/2026-07-12-paldeck-v2.1-release.md`。

## 残余风险

- EXE 未做 Windows Authenticode 代码签名，首次运行仍可能出现 SmartScreen 提示。
- GitHub 与 Nexus 属实时外部服务；本页只证明 2026-07-12 验证时的响应，后续内容和可用性可能变化。
- PyInstaller 报告 Android backend 与 `pycparser.lextab`/`yacctab` 可选 hidden import warning；Windows frozen smoke 已覆盖实际采用的 Edge WebView 路径。
- 上述验证前即存在的 C 盘 dry-run 文本仍保留，但不属于本次构建中间文件。
