# PalDeck v2.3.1 发布验证

日期：2026-07-14

## 范围

- 受管 UE4SS Mod 根级 `config.json` 数值能力检测。
- 严格 `palmod_config.json` schema v1：标签、类型、范围、步长和说明。
- Mod 条目右下角“调整数值 ▼”与内联数字编辑器。
- revision 并发保护、统一游戏写锁、原子 JSON 发布和清单摘要事务更新。
- 不扫描或改写 PAK、Workshop 缓存及任意 Lua 代码中的数字。

## 源码

- 分支：`feature/v2.3.1-mod-value-editor`
- 打包源码提交：`24cde3b7a9da15dc8ca3cc5482089b976109bbba`
- Python：`712 passed, 7 skipped`
- Node：`34 passed`
- JS 语法、Python compileall、`git diff --check`：通过

## 安全覆盖

- 仅接受 UE4SS 清单中唯一根级 `config.json` 和可选 `palmod_config.json`。
- UTF-8 JSON、256 KiB、字段数量、重复 key、NaN/Infinity、类型、范围和步长均严格验证。
- bool 字段可由 schema 声明并验证，但 v2.3.1 数值编辑器不公开。
- reparse、路径越界、非普通文件、未知字段、stale revision 和游戏运行均 fail-closed。
- 非配置受管文件异常时拒绝保存。
- 配置发布后清单保存失败会恢复原配置和原清单；故障注入测试通过。

## 真实 schema 验证

使用当前安装的 `ConfigurableChestSlots` 文件复制到 F 盘临时假游戏目录，data 位于 C 盘：

- 识别字段：`chest_slots`、`guild_chest_slots`、`apply_interval_sec`。
- `chest_slots` 在临时副本中从 50 更新为 51。
- 未提交的 `also_foodbox` 保持不变。
- 保存后 manifest 状态为 `enabled`。
- 未写入真实游戏目录。

## 视觉

- 五页 × `1600×1000`、`1280×820`、`1236×771`、`960×640` 共 20 张。
- 可调整 Mod 的“调整数值 ▼”在四种尺寸均可见，操作按钮自动换行且无横向越界。
- 候选与批准基线差异：20/20 均为 `0.00000000`。

## 可复现构建

两次构建均绑定源码提交 `24cde3b7a9da15dc8ca3cc5482089b976109bbba`：

| 产物 | 两次 SHA-256 | 大小 |
|---|---|---:|
| `PalDeck.exe` | `c0ade2f608845001be1242d2e188f0f69c51da14bdfb70a4784a33d402c18ec9` | 30,432,158 bytes |
| `PalDeck-v2.3.1-windows-portable.zip` | `af07ea05749e3dabd91d0618dc52ad3186a89421755776187091fdf5e0832fc6` | 30,231,704 bytes |

- 两次 EXE/ZIP 大小和 SHA-256 完全一致。
- ZIP sidecar：通过。
- `ZipFile.testzip()`：`None`。
- ZIP 条目：2。

## Frozen smoke

- 真实便携 EXE/WebView：通过。
- `23/23` 项 pass，包括 `mod_value_editor_markers`。
- Fresh data 不配置真实游戏路径，不执行数值写入。
- Loopback listener、窗口句柄、owned process tree 与终止检查通过。

## 残余边界

- 通用 `config.json` 仅公开顶层有限数值；业务有效范围未知时使用绝对安全范围。
- 作者提供 schema 时严格遵守其范围；损坏 schema 不降级通用解析。
- Lua-only 配置 Mod 不显示入口，避免猜测内部变量。
- 仍仅支持 Windows 10/11 Steam 客户端版 Palworld。
