# PalDeck v2.2.2 发布验证

## 修复范围

- Packaged source：`a5d4d54082acbd79b20a7c9d4aa53f291f9a5990`
- 首次读取时自动扫描并接管安装 PalDeck 前已有的本地 Mod。
- 同时扫描 `~mods`、`LogicMods`、classic `Win64/Mods` 与 nested `Win64/ue4ss/Mods`。
- 首次扫描遇到游戏运行时安全延期，不写完成标记；游戏退出后会自动重试。
- 顶部“刷新模组”由清单重读改为真实磁盘重扫，并为大 Mod 库提供 120 秒超时。
- 已有 UE4SS `enabled.txt` 会保持启用语义，不会在接管时被意外禁用。
- 跳过 Okaetsu/UE4SS 随包框架目录，避免将基础组件识别为用户 Mod。
- PAK 扩展名改为大小写不敏感扫描。

## 验证

- Python：`582 passed, 6 skipped`。
- Node：`33 passed`。
- JavaScript 语法、Python compileall 与 `git diff --check` 通过。
- 测试覆盖首次已有 PAK、两种 UE4SS 布局、启用标记保留、游戏运行延期及真实刷新契约。
- 两次全新便携构建完全一致。
- Frozen smoke：18 项全部通过，owned process tree 已终止。
- ZIP CRC 与 sidecar 校验通过。

## 产物

- EXE：`30,349,701 bytes`
- EXE SHA-256：`622a6065331f2fcb4cae9a698bf18b2050845f3772b7f3fb813c6a69ca06429a`
- ZIP：`30,148,808 bytes`
- ZIP SHA-256：`22e7e6efabcc6d2e5ae24f8b63948b4236f473c619fd34e96449079b56eb565c`

## 安全边界

- 不扫描 `Pal/Content/Paks` 根目录，避免把游戏本体 PAK 当作用户 Mod。
- 不递归扫描 PAK 目录，不跟随 reparse point。
- Workshop 仍只管理 Steam 已订阅项目。

## 残余风险

- EXE 未签名，可能触发 SmartScreen。
- 应用内更新仓库仍保持旧仓库；本补丁从 PalDeck Release 手动下载。
